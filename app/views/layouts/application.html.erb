<!DOCTYPE html>
<html>
  <head>
    <title>GmailLike</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="d-flex justify-content-center py-3">
      <ul class="nav nav-pills">
        <li class="nav-item"><a href=<%= root_path %> class="nav-link active" aria-current="page">Home</a></li>
        <%= button_to "Recevoir un email", "/emails", method: "post", class: "btn btn-primary mx-3" %>
        <% if false %>
        <% if user_signed_in? %>
        <li class="nav-item"><a href=<%= destroy_user_session_path%> class="nav-link">Sign out</a></li>
        <li class="nav-item"><a href=<%=edit_user_registration_path %> class="nav-link">Modifier le Profil</a></li>
        <% else %>
        <li class="nav-item"><a href=<%=new_user_session_path %> class="nav-link">Sign in</a></li>
        <li class="nav-item"><a href=<%=new_user_registration_path %> class="nav-link">Sign up</a></li>
        <% end %>
        <% end %>
      </ul>
    </header>
    <%= yield %>
  </body>
</html>
